# blog_aggregator
typescript, docker, drizzle

npm run start gets you started.
To run:
npm run start <command> <argument>

some commands require arguments and if no arguments are listed an error describing format will show up on connsole.
commands:
* login
* register
* reset
* users
* agg
* addfeed
* feeds
* follow
* following
* unfollow
